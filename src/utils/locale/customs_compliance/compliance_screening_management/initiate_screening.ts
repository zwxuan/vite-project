const prefix = 'customs_compliance.compliance_screening_management.initiate_screening';

export const InitiateScreeningLocale = {
  getInitiateScreeningPageTitle() { return `${prefix}.pageTitle`; },

  // Form Sections
  getInitiateScreeningSectionBasic() { return `${prefix}.section.basic`; },
  getInitiateScreeningSectionScope() { return `${prefix}.section.scope`; },
  getInitiateScreeningSectionShipperConsignee() { return `${prefix}.section.shipperConsignee`; },
  getInitiateScreeningSectionCarrier() { return `${prefix}.section.carrier`; },
  getInitiateScreeningSectionGoods() { return `${prefix}.section.goods`; },
  getInitiateScreeningSectionSettings() { return `${prefix}.section.settings`; },

  // Form Fields
  getInitiateScreeningFieldType() { return `${prefix}.field.type`; },
  getInitiateScreeningFieldJobId() { return `${prefix}.field.jobId`; },
  getInitiateScreeningFieldReason() { return `${prefix}.field.reason`; },
  getInitiateScreeningFieldShipper() { return `${prefix}.field.shipper`; },
  getInitiateScreeningFieldConsignee() { return `${prefix}.field.consignee`; },
  getInitiateScreeningFieldCarrier() { return `${prefix}.field.carrier`; },
  getInitiateScreeningFieldGoods() { return `${prefix}.field.goods`; },
  getInitiateScreeningFieldBank() { return `${prefix}.field.bank`; },
  getInitiateScreeningFieldAgent() { return `${prefix}.field.agent`; },

  getInitiateScreeningFieldConsigneeName() { return `${prefix}.field.consigneeName`; },
  getInitiateScreeningFieldConsigneeAddress() { return `${prefix}.field.consigneeAddress`; },
  getInitiateScreeningFieldShipperName() { return `${prefix}.field.shipperName`; },
  getInitiateScreeningFieldShipperAddress() { return `${prefix}.field.shipperAddress`; },

  getInitiateScreeningFieldCarrierName() { return `${prefix}.field.carrierName`; },
  getInitiateScreeningFieldVesselVoyage() { return `${prefix}.field.vesselVoyage`; },
  getInitiateScreeningFieldScac() { return `${prefix}.field.scac`; },

  getInitiateScreeningFieldGoodsName() { return `${prefix}.field.goodsName`; },
  getInitiateScreeningFieldHsCode() { return `${prefix}.field.hsCode`; },
  getInitiateScreeningFieldOrigin() { return `${prefix}.field.origin`; },
  getInitiateScreeningFieldValue() { return `${prefix}.field.value`; },
  getInitiateScreeningFieldUsage() { return `${prefix}.field.usage`; },

  getInitiateScreeningFieldDatabase() { return `${prefix}.field.database`; },
  getInitiateScreeningFieldAccuracy() { return `${prefix}.field.accuracy`; },
  getInitiateScreeningFieldLanguage() { return `${prefix}.field.language`; },
  getInitiateScreeningFieldAutoProcess() { return `${prefix}.field.autoProcess`; },

  // Buttons
  getInitiateScreeningBtnSaveTemplate() { return `${prefix}.btn.saveTemplate`; },
  getInitiateScreeningBtnSubmit() { return `${prefix}.btn.submit`; },
  getInitiateScreeningBtnCancel() { return `${prefix}.btn.cancel`; },
};
